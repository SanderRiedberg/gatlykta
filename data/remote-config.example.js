/* Copy this file to data/remote-config.js and fill in your Supabase
   project URL and the anon (public) key. data/remote-config.js is
   gitignored so credentials don't end up in the repo.

   Find both values in your Supabase project: Settings → API.
   Use the anon/public key, never the service role. */
window.SUPABASE_URL = null;          // 'https://YOUR_PROJECT_REF.supabase.co'
window.SUPABASE_ANON_KEY = null;     // 'YOUR_ANON_PUBLIC_KEY'
